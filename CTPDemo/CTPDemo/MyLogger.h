#pragma once
#include <map>
#include "MyFileMapManager.h"

typedef char shortLog[128];
typedef char normalLog[128];
typedef char normalLog[128];

class MyLogger
{
private:
    MyLogger();
    ~MyLogger();
private:
    static std::map<std::string, MyFileMapManager<
};

